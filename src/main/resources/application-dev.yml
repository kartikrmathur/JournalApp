spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/journaldb}
      database: journaldb
      auto-index-creation: true

server:
  port: 8080
  servlet:
    context-path: /journal